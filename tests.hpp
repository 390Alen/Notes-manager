#ifndef TESTS_HPP
#define TESTS_HPP

#include "notes.hpp"

/**
 * @brief Runs the entire test suite for the application.
 * @param manager A reference to the NoteManager to run tests against.
 * @return True if all tests pass, false otherwise.
 */
bool runAllTests(NoteManager& manager);

#endif // TESTS_HPP